# Known Bugs

Keep confirmed bugs awaiting work in priority order. Each entry contains only
the stable ID, priority, short summary, and link to its authoritative record
under `open/`.

- `BB-BUG-2026-09-15-03` — Medium — Viewer automation PNG capture fails with
  PyGObject 3.46; ordinary graph display is unaffected.
  [Record](open/BB-BUG-2026-09-15-03-gtk-capture-bindings.md).
